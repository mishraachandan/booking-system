<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ======================== -->
    <!--   Console Appender       -->
    <!-- ======================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [traceId=%X{traceId:-none}] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ======================== -->
    <!--   Rolling File Appender  -->
    <!-- ======================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/booking-system.log</file>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [traceId=%X{traceId:-none}] %-5level %logger{50} - %msg%n</pattern>
        </encoder>

        <!-- Roll over daily AND when file exceeds 10 MB -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/booking-system.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- ======================== -->
    <!--   Application Loggers    -->
    <!-- ======================== -->
    <logger name="com.mishraachandan" level="DEBUG" />

    <!-- Hibernate SQL (controlled by spring show-sql, but also here for file) -->
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />

    <!-- Spring framework -->
    <logger name="org.springframework" level="INFO" />

    <!-- ======================== -->
    <!--   Root Logger            -->
    <!-- ======================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
