spring:
  application:
    name: booking-system


  datasource:
    url: jdbc:postgresql://localhost:5433/booking_system
    username: booking_user
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: armaansingh626@gmail.com
    password: uxghnoqvezleeeuv
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
